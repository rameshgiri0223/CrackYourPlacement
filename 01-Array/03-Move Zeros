class Solution {
    public void moveZeroes(int[] nums) {
        //start with first position
        int fPosition = 0;
        for(int i = 0; i<nums.length; i++){
            //Fill all non zero  numbers
            if(nums[i] != 0){
                nums[fPosition] = nums[i];
                fPosition++;
            }
        }
        //Fill all zero
        while(fPosition < nums.length){
            nums[fPosition++] = 0;
        }
        
    }
}
